<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App Theme — Edge-to-Edge:
         windowFullscreen=false + transparente Bars → App zeichnet hinter StatusBar + NavBar.
         Safe-Area-Insets werden via WindowInsetsCompat in MainActivity an CSS weitergeleitet. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <!-- WICHTIG: NICHT true — würde env(safe-area-inset-bottom) auf 0 zwingen -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>

        <!-- Edge-to-Edge: App zeichnet hinter System-Bars -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- WebView Performance -->
        <item name="android:hardwareAccelerated">true</item>
    </style>

    <!-- Launch Theme (SplashScreen) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>
