<<<<<<< HEAD
{
  "appId": "com.hui.app",
  "appName": "HUI",
  "webDir": "www",
  "bundledWebRuntime": false,
  "plugins": {
    "StatusBar": {
      "style": "DEFAULT",
      "backgroundColor": "#00000000",
      "overlaysWebView": true
    }
  },
  "android": {
    "allowMixedContent": false,
    "captureInput": true,
    "webContentsDebuggingEnabled": false
  }
}
=======
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hui.app',
  appName: 'HUI',
  webDir: 'dist',

  // ---- Mobile Performance Optimierung ----
  server: {
    androidScheme: 'https',
    cleartext: false,
  },

  // ---- Android Build Optimierung ----
  android: {
    allowMixedContent: false,

    // Weniger Dateien → schnellerer Build
    path: 'android',

    // Verhindert doppelte Kopien & Sync-Probleme
    buildOptions: {
      keystorePath: 'android/app/release.keystore',
      keystoreAlias: 'hui',
    }
  },

  // ---- Plugins optimieren ----
  plugins: {
    SplashScreen: {
      launchShowDuration: 0,
    },
    CapacitorHttp: {
      enabled: true,
    }
  }
};

export default config;
>>>>>>> 16ac5983 (Auto-Release: Version 2.0.6)
